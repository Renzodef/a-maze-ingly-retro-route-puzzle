# TODO command to configure your environment
## Uncomment to install python's pip
# curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py && python3 get-pip.py && rm get-pip.py
## Uncomment to enable java
# curl -s https://get.sdkman.io | bash && source "$HOME/.sdkman/bin/sdkman-init.sh"
# sdk install java
# source "$HOME/.sdkman/bin/sdkman-init.sh"
## Uncomment to enable kotlin
# curl -s https://get.sdkman.io | bash && source "$HOME/.sdkman/bin/sdkman-init.sh"
# sdk install kotlin
# source "$HOME/.sdkman/bin/sdkman-init.sh"
## Uncomment to enable ruby
# sudo apt update && sudo apt install -y ruby-full
## Uncomment to enable nodejs
# sudo apt update && sudo apt install -y nodejs npm
## Uncomment to enable rust
# curl https://sh.rustup.rs -sSf | sh -s -- -q -y
# source "$HOME/.cargo/env"
echo You have configured the environment successfully!
